services:
  app:
    build: .
    container_name: weekstreak
    ports:
      - "3000:3000"
    environment:
      WEEKSTREAK_DB: /app/data/weekstreak.db
    volumes:
      - weekstreak-data:/app/data
    restart: unless-stopped

volumes:
  weekstreak-data: